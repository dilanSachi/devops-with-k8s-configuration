apiVersion: v1
kind: Service
metadata:
  name: ex410-broadcaster-app-svc
  namespace: project
spec:
  type: ClusterIP
  selector:
    app: ex410-broadcaster-app
  ports:
    - name: broadcaster-app
      port: 2345
      protocol: TCP
      targetPort: 3002
